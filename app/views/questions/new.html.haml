%h1= t("questions.new.headline")

- if flash[:error].present?
  .callout.alert
    %p= flash[:error]

= form_for @question_form, as: :question, url: article_questions_path(@article) do |f|
  = render "questions/partials/form", f: f

  .float-right
    = link_to t("global.cancel"), article_path(@article), class: "button alert"
    = f.button t("questions.new.submit"), class: "button"
